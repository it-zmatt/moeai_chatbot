# API (`apps/api`): copy variables into `apps/api/.env` when running uvicorn from `apps/api`
# (pydantic-settings loads `.env` from the current working directory).
DATABASE_URL=postgresql+asyncpg://USER:PASSWORD@HOST:5432/DATABASE
